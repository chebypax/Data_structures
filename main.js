import SinglyLinkedList from './structures/SinglyLinkedList';

const list = new SinglyLinkedList('hiAll!');
list.push('PRIVET');
list.push(' MY ');
list.push('NIGGER');

console.log(list.pop());
console.log(list.traverse());
